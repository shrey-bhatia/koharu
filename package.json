{
  "scripts": {
    "dev": "cd next && bun run dev",
    "build": "cd next && bun run build"
  },
  "workspaces": [
    "next"
  ],
  "devDependencies": {
    "prettier": "^3.6.2",
    "prettier-plugin-tailwindcss": "^0.6.13"
  }
}
