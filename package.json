{
  "name": "koharu-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "Koharu manga translation tool - pnpm workspace",
  "devDependencies": {
    "@tauri-apps/cli": "^2.7.1",
    "prettier": "^3.6.2",
    "prettier-plugin-tailwindcss": "^0.6.14"
  },
  "scripts": {
    "dev": "pnpm --filter next dev",
    "build": "pnpm --filter next build",
    "clean": "pnpm --recursive exec rm -rf node_modules && rm -rf node_modules"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
