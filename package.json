{
  "scripts": {
    "dev": "cd next && bun run dev",
    "build": "cd next && bun run build",
    "tauri": "tauri"
  },
  "workspaces": [
    "next"
  ],
  "devDependencies": {
    "@tauri-apps/cli": "v2"
  }
}
