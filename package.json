{
  "devDependencies": {
    "prettier": "^3.6.2",
    "prettier-plugin-tailwindcss": "^0.6.13"
  },
  "scripts": {
    "dev": "cd web && bun run dev",
    "build": "cd web && bun run build"
  },
  "workspaces": [
    "web"
  ]
}
